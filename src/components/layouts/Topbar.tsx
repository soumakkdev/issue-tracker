import React from 'react'

export default function Topbar() {
	return (
		<header className="w-full h-12 bg-card flex items-center px-5">
			<p className="font-semibold">Issue Tracker</p>
		</header>
	)
}
